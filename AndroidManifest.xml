<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hsdemo.auction"
    android:versionCode="13"
    android:versionName="1.0.1" >

    <uses-sdk android:minSdkVersion="15" android:targetSdkVersion="21"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.hsdemo.auction.AuctionApplication"
        android:theme="@style/HubSpotTheme"
        android:icon="@drawable/appicon"
        android:label="Auction" >
        <activity
            android:name="com.hsdemo.auction.LoginActivity"
            android:windowSoftInputMode="adjustPan"
            android:label="Auction" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
	    <activity
		    android:name="com.hsdemo.auction.SignupActivity"
		    android:windowSoftInputMode="adjustPan" />

        <activity android:name="com.hsdemo.auction.ItemListActivity"
                  android:windowSoftInputMode="adjustPan"/>

        <receiver android:name="com.hsdemo.auction.GCMReceiver"
                  android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />
                <category android:name="com.hsdemo.auction" />
            </intent-filter>
        </receiver>
	    <service android:name="com.hsdemo.auction.GCMService" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!--
      IMPORTANT: Change "com.hsdemo.auction.permission.C2D_MESSAGE" in the lines below
      to match your app's package name + ".permission.C2D_MESSAGE".
    -->
    <permission android:protectionLevel="signature"
                android:name="com.hsdemo.auction.permission.C2D_MESSAGE" />
    <uses-permission android:name="com.hsdemo.auction.permission.C2D_MESSAGE" />

</manifest>
